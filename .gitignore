/target
.gitnexus
.repos/effect
codedb.snapshot
.worktrees/

# Secrets and local state — never commit
*.sops.yaml
*.sops.yml
*.sops.json
age.key
*.age
.env
.env.*
*.sqlite
*.sqlite3
*.db
*.pem
*.key
# minisign release-signing keypair (ADR-029). The secret key must NEVER be
# committed; store it as the MINISIGN_SECRET_KEY GitHub Actions secret.
key.sec
key.pub
*.minisig

TODO.md